<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://domagojhack.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://domagojhack.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-07T17:41:22+00:00</updated><id>https://domagojhack.github.io/feed.xml</id><title type="html">blank</title><subtitle>Computational ecology </subtitle><entry><title type="html">Knowledge graphs and LLMs — a new instrument for science</title><link href="https://domagojhack.github.io/blog/2026/knowledge-graphs/" rel="alternate" type="text/html" title="Knowledge graphs and LLMs — a new instrument for science"/><published>2026-05-07T00:00:00+00:00</published><updated>2026-05-07T00:00:00+00:00</updated><id>https://domagojhack.github.io/blog/2026/knowledge-graphs</id><content type="html" xml:base="https://domagojhack.github.io/blog/2026/knowledge-graphs/"><![CDATA[<p>Science has always had two problems: generating knowledge and connecting it. The first has accelerated dramatically — genomics, remote sensing, ecological monitoring, and automated literature production are generating data faster than any field can absorb. The second problem has barely moved. The connections between a species observation in Croatia, a soil microbiology paper from 2019, a climate anomaly recorded in the Copernicus archive, and a gene expression dataset from a related experiment in Japan exist — but they live in separate databases, separate formats, and separate vocabularies. No researcher can traverse them. No search engine indexes their relationships. The knowledge is there, but it cannot be asked.</p> <p>That is beginning to change. The combination of <strong>knowledge graphs</strong> and <strong>large language models</strong> is creating something genuinely new: the ability to pose intricate scientific questions across vast, heterogeneous bodies of knowledge and receive grounded, traceable answers. Not summaries of what individual documents say, but synthesised reasoning over the <em>structure</em> of what we know. This post argues that this combination is not an incremental improvement in literature search — it is a qualitatively different way of interacting with scientific knowledge, and biology is one of the domains where it will matter most.</p> <h2 id="the-problem-with-how-science-stores-knowledge">The problem with how science stores knowledge</h2> <p>Consider a question that any ecologist might ask:</p> <blockquote> <p><em>Which soil invertebrate taxa, recorded in post-flood Pannonian wetlands, have functional traits associated with organic matter decomposition, and what does the literature say about their resilience to drought stress?</em></p> </blockquote> <p>This question requires simultaneously traversing:</p> <ul> <li>A taxonomic hierarchy (identifying the relevant taxa)</li> <li>An occurrence database (filtering by habitat and geography)</li> <li>A trait database (linking taxa to functional roles)</li> <li>A climate event database (identifying the relevant disturbance history)</li> <li>The primary literature (synthesising what is known about drought resilience)</li> </ul> <p>Today, answering this takes weeks of manual work — downloading from GBIF, cross-referencing trait databases, running literature searches, resolving taxonomic synonyms across sources, then synthesising by hand. The bottleneck is not data. The bottleneck is the absence of a structure that connects these layers and a tool that can reason across them.</p> <p>Knowledge graphs are the structure. LLMs are now becoming the reasoning layer on top.</p> <h2 id="what-a-knowledge-graph-actually-is">What a knowledge graph actually is</h2> <p>A knowledge graph stores information as a network of entities and typed relationships between them. Every statement is a <strong>triple</strong>:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>(subject)  —  [predicate]  →  (object)
</code></pre></div></div> <p>The power is in the predicate. Generic databases store rows. Knowledge graphs store <em>meaning</em>:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>(Lumbricus terrestris) — [belongs_to]      → (Lumbricidae)
(Lumbricus terrestris) — [functional_group] → (decomposer)
(Lumbricus terrestris) — [feeds_on]        → (organic_litter)
(Lumbricus terrestris) — [is_prey_of]      → (Turdus merula)
(Lumbricus terrestris) — [sensitive_to]    → (soil_compaction)
(plot_A)               — [located_in]      → (Kopački rit)
(plot_A)               — [soil_type]       → (fluvisol)
(Kopački rit)          — [is_a]            → (Ramsar_wetland)
(Kopački rit)          — [experienced]     → (2022_drought_event)
(obs_042)              — [records]         → (Lumbricus terrestris)
(obs_042)              — [at_location]     → (plot_A)
(obs_042)              — [on_date]         → (2023-04-15)
</code></pre></div></div> <p>Now ask: <em>Which decomposer species were observed in Ramsar wetlands after a drought event, at sites with fluvisol soils?</em> A graph engine traverses this in one query. A relational database would need four or five JOINs across tables that probably do not exist in a single schema. A language model asked cold would likely hallucinate a plausible but unverifiable answer.</p> <p>The graph makes the question answerable <em>and</em> the answer auditable — every step in the reasoning path is a named edge that can be inspected and corrected.</p> <h2 id="biology-as-a-knowledge-graph--the-infrastructure-already-exists">Biology as a knowledge graph — the infrastructure already exists</h2> <p>Here is what makes biology particularly well-positioned for this transition: the field has been building knowledge graph infrastructure for decades, largely without calling it that.</p> <p><strong>Taxonomic hierarchies</strong> are directed graphs. The NCBI Taxonomy — the backbone used by GenBank, GBIF, and most biodiversity informatics platforms — contains over 2.3 million named taxa connected by <code class="language-plaintext highlighter-rouge">is_a</code> and <code class="language-plaintext highlighter-rouge">part_of</code> relations into a single rooted tree. Every accession number in GenBank is a node attached to this graph.</p> <p><strong>The Gene Ontology (GO)</strong> is one of the most successful biological knowledge graphs ever built. It defines three structured vocabularies — molecular function, biological process, cellular component — and connects them through <code class="language-plaintext highlighter-rouge">is_a</code>, <code class="language-plaintext highlighter-rouge">part_of</code>, and <code class="language-plaintext highlighter-rouge">regulates</code> relations. Tens of millions of gene product annotations in every model organism database are expressed as triples against this ontology.</p> <p><strong>The OBO Foundry</strong> provides a suite of interoperable biological ontologies that collectively cover the vocabulary of the life sciences:</p> <table> <thead> <tr> <th>Ontology</th> <th>What it models</th> </tr> </thead> <tbody> <tr> <td>NCBI Taxonomy</td> <td>All described taxa and their hierarchical relationships</td> </tr> <tr> <td>Gene Ontology (GO)</td> <td>Molecular function, biological process, cellular component</td> </tr> <tr> <td>Environment Ontology (ENVO)</td> <td>Habitats, biomes, environmental materials</td> </tr> <tr> <td>Relation Ontology (RO)</td> <td>Standardised biological predicates (<code class="language-plaintext highlighter-rouge">eats</code>, <code class="language-plaintext highlighter-rouge">parasitizes</code>, <code class="language-plaintext highlighter-rouge">located_in</code>, …)</td> </tr> <tr> <td>Phenotype and Trait Ontology (PATO)</td> <td>Organism qualities and phenotypic descriptions</td> </tr> <tr> <td>Darwin Core</td> <td>Occurrence records, sampling events, measurements</td> </tr> </tbody> </table> <p><strong>GBIF</strong> is the world’s largest biodiversity knowledge graph in operational use. Its backbone taxonomy links over 9 million species names through synonymy and hierarchy. Every occurrence record links a taxon node to a location, a date, an observer, a dataset, and an institution. At full scale, this amounts to hundreds of millions of triples, queryable through APIs and SPARQL endpoints.</p> <p>The raw material for a biological knowledge graph at civilisational scale already exists. What has been missing is the reasoning layer that makes it explorable by scientists asking natural-language questions.</p> <h2 id="what-llms-change">What LLMs change</h2> <p>A language model alone is a poor tool for scientific reasoning. It has absorbed enormous amounts of biological literature, but it cannot reliably distinguish what it knows from what it is confabulating. Ask it about the drought tolerance of a specific earthworm species in a specific bioregion and it will give you an answer — fluent, confident, and quite possibly wrong.</p> <p>A knowledge graph alone is also limited. You can query it with precision, but only for questions you have already thought to ask in the exact structure the schema supports. It does not synthesise. It does not generalise. It does not bridge from a precise factual retrieval to the broader scientific context.</p> <p>Together, they compensate for each other’s weaknesses.</p> <p>The architecture that makes this work is called <strong>graph-augmented retrieval</strong> (a specialisation of RAG). The idea is to use the language model’s ability to understand a natural language question, convert it into a graph query, retrieve a relevant subgraph, and then reason over that grounded context rather than over parametric memory alone:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Natural language question
        │
        ▼
  LLM parses intent → structured graph query
        │
        ▼
  Graph traversal → relevant subgraph (verified triples)
        │
        ▼
  LLM reasons over subgraph → answer with traceable provenance
</code></pre></div></div> <p>The result is qualitatively different from a search engine hit or a plain LLM response. Every claim in the answer is backed by a specific triple in a specific database. When the answer is wrong, the error is in the graph — and graphs can be corrected, versioned, and maintained without retraining the model.</p> <h2 id="the-scientific-questions-this-enables">The scientific questions this enables</h2> <p>What changes when you have this capability is the <em>scale and intricacy</em> of questions that become answerable in a single session rather than a multi-week research project.</p> <p><strong>Cross-domain synthesis.</strong> A question like <em>“Which invertebrate taxa associated with soil bioturbation in European wetlands also appear in the IUCN threatened species list, and what do the last five years of primary literature say about the mechanisms of their vulnerability?”</em> previously required a team of researchers and months of work. A knowledge graph connecting GBIF occurrences, functional trait databases, the IUCN Red List, and a literature index makes this a single traversal followed by a synthesising prompt.</p> <p><strong>Hypothesis generation at scale.</strong> Knowledge graphs make it possible to systematically scan for structural patterns — species with similar trait profiles that have diverged in extinction risk, ecological network motifs that correlate with resilience, genes that participate in the same biological process across distantly related organisms. These are not questions with known answers that you search for. They are patterns in the <em>structure</em> of the knowledge that only become visible when the knowledge is formally connected.</p> <p><strong>Integrating heterogeneous data.</strong> A mosquito vector surveillance graph can connect occurrence records, host range data, pathogen associations, land-use change layers, and climate projections into a single queryable structure. The question <em>“In which areas of the Adriatic coast is the projected range expansion of Aedes albopictus likely to intersect with populations of immunocompromised hosts and existing gaps in surveillance?”</em> is a multi-hop graph query, not a keyword search. The answer requires simultaneously reasoning over taxonomy, medical demography, spatial data, and climate models.</p> <p><strong>Literature as structured knowledge.</strong> LLMs can now extract triples from unstructured text with reasonable accuracy — meaning that published papers, field reports, and grey literature can be progressively converted into graph edges. A knowledge graph that grows by ingesting the literature is a form of machine-assisted knowledge accumulation that does not depend on anyone manually curating every relationship.</p> <h2 id="building-it-in-practice">Building it in practice</h2> <p>The entry point in Python is straightforward. Starting from a GBIF occurrence export:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">networkx</span> <span class="k">as</span> <span class="n">nx</span>
<span class="kn">import</span> <span class="n">pandas</span> <span class="k">as</span> <span class="n">pd</span>

<span class="n">occ</span> <span class="o">=</span> <span class="n">pd</span><span class="p">.</span><span class="nf">read_csv</span><span class="p">(</span><span class="sh">"</span><span class="s">occurrences.csv</span><span class="sh">"</span><span class="p">)</span>
<span class="n">G</span> <span class="o">=</span> <span class="n">nx</span><span class="p">.</span><span class="nc">MultiDiGraph</span><span class="p">()</span>

<span class="k">for</span> <span class="n">_</span><span class="p">,</span> <span class="n">row</span> <span class="ow">in</span> <span class="n">occ</span><span class="p">.</span><span class="nf">iterrows</span><span class="p">():</span>
    <span class="n">sp</span> <span class="o">=</span> <span class="n">row</span><span class="p">[</span><span class="sh">"</span><span class="s">species</span><span class="sh">"</span><span class="p">]</span>

    <span class="c1"># Taxonomic chain
</span>    <span class="k">for</span> <span class="n">child</span><span class="p">,</span> <span class="n">parent</span><span class="p">,</span> <span class="n">rank</span> <span class="ow">in</span> <span class="p">[</span>
        <span class="p">(</span><span class="n">sp</span><span class="p">,</span>             <span class="n">row</span><span class="p">[</span><span class="sh">"</span><span class="s">family</span><span class="sh">"</span><span class="p">],</span> <span class="sh">"</span><span class="s">belongs_to</span><span class="sh">"</span><span class="p">),</span>
        <span class="p">(</span><span class="n">row</span><span class="p">[</span><span class="sh">"</span><span class="s">family</span><span class="sh">"</span><span class="p">],</span>  <span class="n">row</span><span class="p">[</span><span class="sh">"</span><span class="s">order</span><span class="sh">"</span><span class="p">],</span>  <span class="sh">"</span><span class="s">belongs_to</span><span class="sh">"</span><span class="p">),</span>
        <span class="p">(</span><span class="n">row</span><span class="p">[</span><span class="sh">"</span><span class="s">order</span><span class="sh">"</span><span class="p">],</span>   <span class="n">row</span><span class="p">[</span><span class="sh">"</span><span class="s">class</span><span class="sh">"</span><span class="p">],</span>  <span class="sh">"</span><span class="s">belongs_to</span><span class="sh">"</span><span class="p">),</span>
    <span class="p">]:</span>
        <span class="n">G</span><span class="p">.</span><span class="nf">add_node</span><span class="p">(</span><span class="n">child</span><span class="p">,</span> <span class="n">node_type</span><span class="o">=</span><span class="n">rank</span><span class="p">)</span>
        <span class="n">G</span><span class="p">.</span><span class="nf">add_edge</span><span class="p">(</span><span class="n">child</span><span class="p">,</span> <span class="n">parent</span><span class="p">,</span> <span class="n">relation</span><span class="o">=</span><span class="sh">"</span><span class="s">belongs_to</span><span class="sh">"</span><span class="p">)</span>

    <span class="c1"># Observation
</span>    <span class="n">obs</span> <span class="o">=</span> <span class="sa">f</span><span class="sh">"</span><span class="s">obs_</span><span class="si">{</span><span class="n">row</span><span class="p">.</span><span class="n">name</span><span class="si">}</span><span class="sh">"</span>
    <span class="n">G</span><span class="p">.</span><span class="nf">add_node</span><span class="p">(</span><span class="n">obs</span><span class="p">,</span> <span class="n">node_type</span><span class="o">=</span><span class="sh">"</span><span class="s">observation</span><span class="sh">"</span><span class="p">,</span>
               <span class="n">count</span><span class="o">=</span><span class="n">row</span><span class="p">[</span><span class="sh">"</span><span class="s">individualCount</span><span class="sh">"</span><span class="p">],</span> <span class="n">date</span><span class="o">=</span><span class="n">row</span><span class="p">[</span><span class="sh">"</span><span class="s">eventDate</span><span class="sh">"</span><span class="p">])</span>
    <span class="n">G</span><span class="p">.</span><span class="nf">add_edge</span><span class="p">(</span><span class="n">obs</span><span class="p">,</span> <span class="n">sp</span><span class="p">,</span>             <span class="n">relation</span><span class="o">=</span><span class="sh">"</span><span class="s">records</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">G</span><span class="p">.</span><span class="nf">add_edge</span><span class="p">(</span><span class="n">obs</span><span class="p">,</span> <span class="n">row</span><span class="p">[</span><span class="sh">"</span><span class="s">locality</span><span class="sh">"</span><span class="p">],</span> <span class="n">relation</span><span class="o">=</span><span class="sh">"</span><span class="s">at_location</span><span class="sh">"</span><span class="p">)</span>

    <span class="c1"># Habitat
</span>    <span class="k">if</span> <span class="n">pd</span><span class="p">.</span><span class="nf">notna</span><span class="p">(</span><span class="n">row</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">habitat</span><span class="sh">"</span><span class="p">)):</span>
        <span class="n">G</span><span class="p">.</span><span class="nf">add_edge</span><span class="p">(</span><span class="n">sp</span><span class="p">,</span> <span class="n">row</span><span class="p">[</span><span class="sh">"</span><span class="s">habitat</span><span class="sh">"</span><span class="p">],</span> <span class="n">relation</span><span class="o">=</span><span class="sh">"</span><span class="s">found_in</span><span class="sh">"</span><span class="p">)</span>

<span class="c1"># Multi-hop: all families recorded in fluvisol habitats
</span><span class="n">fluvisol_species</span> <span class="o">=</span> <span class="p">{</span>
    <span class="n">u</span> <span class="k">for</span> <span class="n">u</span><span class="p">,</span> <span class="n">v</span><span class="p">,</span> <span class="n">d</span> <span class="ow">in</span> <span class="n">G</span><span class="p">.</span><span class="nf">in_edges</span><span class="p">(</span><span class="sh">"</span><span class="s">fluvisol</span><span class="sh">"</span><span class="p">,</span> <span class="n">data</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">d</span><span class="p">[</span><span class="sh">"</span><span class="s">relation</span><span class="sh">"</span><span class="p">]</span> <span class="o">==</span> <span class="sh">"</span><span class="s">found_in</span><span class="sh">"</span>
<span class="p">}</span>
<span class="n">families</span> <span class="o">=</span> <span class="p">{</span>
    <span class="n">v</span> <span class="k">for</span> <span class="n">sp</span> <span class="ow">in</span> <span class="n">fluvisol_species</span>
    <span class="k">for</span> <span class="n">_</span><span class="p">,</span> <span class="n">v</span><span class="p">,</span> <span class="n">d</span> <span class="ow">in</span> <span class="n">G</span><span class="p">.</span><span class="nf">out_edges</span><span class="p">(</span><span class="n">sp</span><span class="p">,</span> <span class="n">data</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">d</span><span class="p">[</span><span class="sh">"</span><span class="s">relation</span><span class="sh">"</span><span class="p">]</span> <span class="o">==</span> <span class="sh">"</span><span class="s">belongs_to</span><span class="sh">"</span>
<span class="p">}</span>
</code></pre></div></div> <p>Once the graph exists, dropping in a LangChain graph retriever against it — or exporting to Neo4j for Cypher queries — adds the natural language layer. The graph does not need to be complete to be useful; even a partial connection of occurrence data, taxonomy, and a trait table opens up queries that were previously infeasible.</p> <h2 id="a-different-relationship-with-knowledge">A different relationship with knowledge</h2> <p>What is at stake here is not a better literature search. It is a different relationship between a scientist and the total body of knowledge in their field.</p> <p>Today, a researcher’s effective knowledge is bounded by what they have personally read, what their collaborators know, and what a keyword search surfaces. Knowledge graphs with LLM reasoning layers extend that boundary considerably. They make it possible to reason over connections that no single person has made, to find patterns in the structure of knowledge rather than in individual documents, and to ask questions at a scale and complexity that would previously have required a dedicated research programme.</p> <p>For biology — a field sitting on top of the largest, most structurally complex, most rapidly growing knowledge base in science — this matters acutely. The species interactions, taxonomic relationships, functional traits, genomic annotations, ecological observations, and climate linkages are all there. The ontologies that define their vocabulary are maintained. The databases that hold the data are publicly accessible. What has been missing is the capacity to reason over all of it simultaneously.</p> <p>That capacity is now, finally, becoming available.</p>]]></content><author><name></name></author><category term="ai"/><category term="knowledge graph"/><category term="LLM"/><category term="RAG"/><category term="ontology"/><category term="ecology"/><category term="biodiversity"/><category term="scientific computing"/><summary type="html"><![CDATA[New techniques in LLM-powered knowledge retrieval are making knowledge graphs essential for scientific exploration — enabling intricate inquiries and a fundamentally different way of reasoning over knowledge at scale.]]></summary></entry><entry><title type="html">The essential toolkit</title><link href="https://domagojhack.github.io/blog/2025/toolkit/" rel="alternate" type="text/html" title="The essential toolkit"/><published>2025-07-20T00:00:00+00:00</published><updated>2025-07-20T00:00:00+00:00</updated><id>https://domagojhack.github.io/blog/2025/toolkit</id><content type="html" xml:base="https://domagojhack.github.io/blog/2025/toolkit/"><![CDATA[<p>I embarked on my Python programming journey in 2010, during my freshman year. Over time the work has grown from simple scripts to a sprawling stack covering geospatial analysis, deep learning, LLM-powered agents, and production web APIs. With each passing year the toolkit gets refined — libraries that once felt exotic become daily drivers, and new ones earn their place only after proving their worth across multiple projects.</p> <p>This post is a living record of what I actually reach for. Not a curated “top 10” list, but an honest survey of the packages that show up in my <code class="language-plaintext highlighter-rouge">environment.yml</code> files again and again.</p> <h2 id="setup">Setup</h2> <p>I manage all Python environments with <strong>Miniforge</strong> — the community-maintained, conda-forge distribution of conda. It is lightweight, defaults to the vastly richer <code class="language-plaintext highlighter-rouge">conda-forge</code> channel, and lets me pin exact environments per project through <code class="language-plaintext highlighter-rouge">environment.yml</code> files that I commit alongside the code.</p> <p>Python has three package managers worth knowing: <code class="language-plaintext highlighter-rouge">pip</code>, <code class="language-plaintext highlighter-rouge">conda</code>, and <code class="language-plaintext highlighter-rouge">mamba</code>.</p> <table> <thead> <tr> <th> </th> <th style="text-align: center">pip</th> <th style="text-align: center">conda</th> <th style="text-align: center">mamba</th> </tr> </thead> <tbody> <tr> <td>Library manager</td> <td style="text-align: center">✔️</td> <td style="text-align: center">✔️</td> <td style="text-align: center">✔️</td> </tr> <tr> <td>Virtual environment manager</td> <td style="text-align: center">❌</td> <td style="text-align: center">✔️</td> <td style="text-align: center">✔️</td> </tr> <tr> <td>Dependency conflict solver</td> <td style="text-align: center">❌</td> <td style="text-align: center">✔️</td> <td style="text-align: center">✔️</td> </tr> <tr> <td>Official PyPI support</td> <td style="text-align: center">✔️</td> <td style="text-align: center">❌</td> <td style="text-align: center">❌</td> </tr> <tr> <td>Scientific / binary packages</td> <td style="text-align: center">❌</td> <td style="text-align: center">✔️</td> <td style="text-align: center">✔️</td> </tr> <tr> <td>Parallel solver</td> <td style="text-align: center">❌</td> <td style="text-align: center">❌</td> <td style="text-align: center">✔️</td> </tr> </tbody> </table> <p>In practice: <code class="language-plaintext highlighter-rouge">conda</code>/<code class="language-plaintext highlighter-rouge">mamba</code> for environments and compiled dependencies (GDAL, PyTorch CUDA builds, HDF5), <code class="language-plaintext highlighter-rouge">pip</code> for anything that only lives on PyPI.</p> <p>Install Miniforge on Linux:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-L</span> <span class="nt">-O</span> <span class="s2">"https://github.com/conda-forge/miniforge/releases/latest/download/Miniforge3-</span><span class="si">$(</span><span class="nb">uname</span><span class="si">)</span><span class="s2">-</span><span class="si">$(</span><span class="nb">uname</span> <span class="nt">-m</span><span class="si">)</span><span class="s2">.sh"</span>
bash Miniforge3-<span class="si">$(</span><span class="nb">uname</span><span class="si">)</span>-<span class="si">$(</span><span class="nb">uname</span> <span class="nt">-m</span><span class="si">)</span>.sh
</code></pre></div></div> <p>Then install <code class="language-plaintext highlighter-rouge">mamba</code> for faster solves:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda <span class="nb">install</span> <span class="nt">-n</span> base mamba <span class="nt">-c</span> conda-forge
</code></pre></div></div> <p>Create and activate a project environment:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mamba create <span class="nt">-n</span> myenv <span class="nv">python</span><span class="o">=</span>3.11
conda activate myenv
</code></pre></div></div> <h3 id="why-environments-matter">Why environments matter</h3> <p>Every project in <code class="language-plaintext highlighter-rouge">~/dev</code> lives in its own conda environment. A geospatial project might pin <code class="language-plaintext highlighter-rouge">gdal=3.8</code>, while a PyTorch project needs CUDA 12.x binaries — these cannot coexist in a single global install. Committing <code class="language-plaintext highlighter-rouge">environment.yml</code> also means any collaborator (or a future me) can reproduce the exact setup with one command:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mamba <span class="nb">env </span>create <span class="nt">-f</span> environment.yml
</code></pre></div></div> <hr/> <h2 id="the-toolkit">The toolkit</h2> <h3 id="core-scientific-stack">Core scientific stack</h3> <p>These appear in virtually every project.</p> <p><strong>NumPy</strong> is the foundation — n-dimensional arrays, broadcasting, and the data model that everything else builds on. You rarely call it alone, but nothing works without it.</p> <p><strong>SciPy</strong> adds the algorithms: integration, interpolation, signal processing, statistics, sparse matrices, and spatial data structures. I reach for <code class="language-plaintext highlighter-rouge">scipy.stats</code>, <code class="language-plaintext highlighter-rouge">scipy.optimize</code>, and <code class="language-plaintext highlighter-rouge">scipy.spatial</code> constantly.</p> <p><strong>Pandas</strong> handles tabular data. DataFrames are how field data, CSV exports, database query results, and API responses all end up in memory. The <code class="language-plaintext highlighter-rouge">groupby</code> / <code class="language-plaintext highlighter-rouge">apply</code> / <code class="language-plaintext highlighter-rouge">merge</code> pattern covers 80 % of data wrangling.</p> <p><strong>Matplotlib</strong> and <strong>Seaborn</strong> cover publication-quality static plots. Seaborn’s statistical plot types (<code class="language-plaintext highlighter-rouge">boxplot</code>, <code class="language-plaintext highlighter-rouge">violinplot</code>, <code class="language-plaintext highlighter-rouge">pairplot</code>) save a lot of boilerplate; Matplotlib gives the fine-grained control needed for figures that go into papers.</p> <p><strong>Tqdm</strong> wraps any iterable in a progress bar. Indispensable when a loop takes more than a few seconds.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mamba <span class="nb">install </span>numpy scipy pandas matplotlib seaborn tqdm
</code></pre></div></div> <hr/> <h3 id="geospatial-stack">Geospatial stack</h3> <p>A large part of my work involves spatial data — remote sensing products, habitat maps, drone surveys, climate model output.</p> <p><strong>GeoPandas</strong> extends Pandas with a geometry column, making it possible to do spatial joins, reprojections, and area calculations using a DataFrame interface. It is the entry point for most vector data workflows.</p> <p><strong>Rasterio</strong> and <strong>GDAL</strong> handle raster data: reading GeoTIFF and NetCDF files, reprojecting, windowed reads for large datasets. GDAL is the underlying engine for most geospatial I/O across languages.</p> <p><strong>Shapely</strong> provides the geometric primitives (polygons, lines, points) and the operations on them (union, intersection, buffer, distance). GeoPandas uses it internally but I often need it directly.</p> <p><strong>xarray</strong> is indispensable for multidimensional scientific datasets — especially NetCDF climate model output and satellite products with (time, lat, lon) or (time, level, lat, lon) dimensions. It brings Pandas-like labelled indexing to arrays, and integrates well with <code class="language-plaintext highlighter-rouge">cfgrib</code> for ECMWF GRIB files.</p> <p><strong>Cartopy</strong> produces publication-quality maps with proper projections. For interactive web maps I reach for <strong>Folium</strong> or hand the GeoJSON off to a Leaflet frontend.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mamba <span class="nb">install </span>geopandas rasterio gdal shapely xarray cartopy folium
</code></pre></div></div> <hr/> <h3 id="computer-vision-and-image-processing">Computer vision and image processing</h3> <p>Several projects involve microscopy images, UAV imagery, and camera trap footage.</p> <p><strong>OpenCV</strong> covers the low-level operations: colour space conversion, morphological transforms, contour detection, optical flow. It is fast, battle-tested, and has bindings for almost every language.</p> <p><strong>Pillow</strong> and <strong>scikit-image</strong> handle higher-level image tasks: format conversion, filtering, feature detection, region properties. I use <code class="language-plaintext highlighter-rouge">scikit-image</code> when I need something that feels more NumPy-native.</p> <p><strong>PyTorch</strong> with <strong>TorchVision</strong> is the framework I use for training and running deep learning models. The <strong>Ultralytics</strong> package wraps YOLO models with a clean API for object detection and instance segmentation, which comes up frequently in ecological monitoring applications. For cell and nucleus segmentation in microscopy, <strong>Cellpose</strong> has become the default.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mamba <span class="nb">install </span>pytorch torchvision pytorch-cuda<span class="o">=</span>12.1 <span class="nt">-c</span> pytorch <span class="nt">-c</span> nvidia
pip <span class="nb">install </span>ultralytics cellpose opencv-python scikit-image
</code></pre></div></div> <hr/> <h3 id="llm-and-rag">LLM and RAG</h3> <p>The last two years have added a new layer to the stack.</p> <p><strong>LangChain</strong> is my go-to orchestration framework for LLM-powered applications. It handles prompt templates, chains, tool use, and memory, and it integrates cleanly with local and hosted models.</p> <p><strong>Ollama</strong> runs models locally — Llama, Mistral, Phi, Gemma — which matters when data cannot leave the machine. For hosted inference I use the <strong>OpenAI</strong> API.</p> <p><strong>ChromaDB</strong> is a lightweight vector database for retrieval-augmented generation (RAG). Embeddings are generated with <strong>Sentence Transformers</strong> from HuggingFace and stored in Chroma, making it possible to build document Q&amp;A systems that run entirely offline.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip <span class="nb">install </span>langchain langchain-community chromadb sentence-transformers openai
</code></pre></div></div> <hr/> <h3 id="web-and-api-development">Web and API development</h3> <p>Many projects end up as services — data APIs, processing backends, interactive dashboards.</p> <p><strong>FastAPI</strong> is my Python web framework of choice. It generates OpenAPI docs automatically, validates request/response bodies via Pydantic, and is fast enough for production workloads. Paired with <strong>Uvicorn</strong> as the ASGI server, it handles async endpoints naturally.</p> <p><strong>Pydantic</strong> (v2) does data validation and settings management. I use <code class="language-plaintext highlighter-rouge">BaseModel</code> for API schemas and <code class="language-plaintext highlighter-rouge">BaseSettings</code> with <code class="language-plaintext highlighter-rouge">python-dotenv</code> to load configuration from <code class="language-plaintext highlighter-rouge">.env</code> files without touching the codebase.</p> <p><strong>Motor</strong> is the async MongoDB driver. MongoDB works well for document-heavy applications — nested ecological records, agent memory stores, API response caches — where a rigid relational schema would require too many migrations.</p> <p><strong>Streamlit</strong> is the quickest path from a Python script to a shareable interactive dashboard, useful for internal tools and exploratory data apps that do not need a full frontend.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip <span class="nb">install </span>fastapi uvicorn[standard] pydantic pydantic-settings motor python-dotenv streamlit
</code></pre></div></div> <hr/> <h3 id="code-quality">Code quality</h3> <p>A consistent codebase lowers the cognitive load when context-switching between projects.</p> <p><strong>Ruff</strong> is a fast Python linter and formatter (written in Rust) that replaces Flake8, isort, and most of what Black does — in one tool. I configure it once in <code class="language-plaintext highlighter-rouge">pyproject.toml</code> and let the VS Code extension apply it on save.</p> <p><strong>MyPy</strong> for gradual static typing on projects that grow large enough to benefit from it. Not every script needs type annotations, but FastAPI models and anything with a public API should be typed.</p> <p><strong>Pytest</strong> with <strong>pytest-asyncio</strong> for testing. Writing tests for FastAPI endpoints and async database operations is straightforward with these two together.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip <span class="nb">install </span>ruff mypy pytest pytest-asyncio
</code></pre></div></div> <hr/> <h3 id="editor-visual-studio-code">Editor: Visual Studio Code</h3> <p>All of the above runs inside <strong>Visual Studio Code</strong>. The Python extension (Microsoft), Pylance for type inference, the Ruff extension for linting, and the Jupyter extension for notebooks cover the workflow. The built-in terminal, Git panel, and Dev Containers extension for Docker-based environments mean I rarely need to leave the editor.</p> <hr/> <p>The toolkit has grown considerably since the early Python days. What has stayed constant is the philosophy: use the best tool for the problem, keep environments isolated, and document dependencies so the work is reproducible. The specific packages will keep changing; the practices around them are what actually matter.</p> <blockquote> <p>Originally posted July 27, 2025 — updated May 2026</p> </blockquote>]]></content><author><name></name></author><category term="workflow"/><category term="python"/><category term="toolkit"/><category term="pandas"/><category term="numpy"/><category term="pytorch"/><category term="fastapi"/><category term="geopandas"/><category term="langchain"/><category term="conda"/><category term="visual studio code"/><summary type="html"><![CDATA[I embarked on my Python programming journey in 2010, during my freshman year. Over time the work has grown from simple scripts to a sprawling stack covering geospatial analysis, deep learning, LLM-powered agents, and production web APIs. With each passing year the toolkit gets refined — libraries that once felt exotic become daily drivers, and new ones earn their place only after proving their worth across multiple projects.]]></summary></entry></feed>